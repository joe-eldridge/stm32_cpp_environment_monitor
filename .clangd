# Firmware sources use the cross build's compile database; the host-side unit
# tests use their own (configure it with `cmake -S tests -B build/tests`).
If:
  PathExclude: tests/.*
CompileFlags:
  CompilationDatabase: build/Debug
---
If:
  PathMatch: tests/.*
CompileFlags:
  CompilationDatabase: build/tests
